# - Create an array variable named `numList` with the following content: 
#   `[3, 4, 5, 6, 7]`
# - Double all the values in the array

numList= [3,4,5,6,7]

numList= [x*2 for x in  numList]

print(numList)
